services:
  - type: web
    name: plot-analyzer
    runtime: node
    plan: starter
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
